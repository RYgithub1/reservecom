import 'package:flutter/material.dart';

class Facility {
  String facilityText;
  IconData facilityIconData;
  bool isSelected;
  Facility(this.facilityText, this.facilityIconData, this.isSelected);
}
